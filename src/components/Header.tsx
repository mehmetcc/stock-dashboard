const Header: React.FC = () => {
    return (
        <header className="w-full bg-blue-600 p-4 text-white text-center">
            <h1 className="text-xl font-bold">Mehmet's Stock Price Dashboard</h1>
        </header>
    );
};

export default Header;
